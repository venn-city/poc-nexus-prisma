"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var graphql_1 = require("../graphql");
var resolver_1 = require("../resolver");
var common_1 = require("./common");
function objectTypeToNexus(builder, type, prismaClient, datamodelInfo) {
    var nexusFieldsConfig = objectTypeFieldsToNexus(type, prismaClient, datamodelInfo);
    return builder.buildObjectType({
        name: type.name,
        definition: function (t) {
            Object.entries(nexusFieldsConfig).forEach(function (_a) {
                var name = _a[0], config = _a[1];
                t.field(name, config);
            });
            type.getInterfaces().forEach(function (interfaceType) {
                t.implements(interfaceType.name);
            });
        },
    });
}
exports.objectTypeToNexus = objectTypeToNexus;
function objectTypeFieldToNexus(typeName, field, prismaClient, datamodelInfo) {
    return __assign({}, common_1.graphqlTypeToCommonNexus(field), { type: graphql_1.getTypeName(field.type), resolve: resolver_1.generateDefaultResolver(typeName, field, prismaClient, datamodelInfo), args: common_1.graphqlArgsToNexusArgs(field.args) });
}
function objectTypeFieldsToNexus(type, prismaClient, datamodelInfo) {
    var fields = Object.values(type.getFields());
    // TODO: Remove that once `node` is removed from the Prisma API
    if (type.name === 'Query') {
        fields = fields.filter(function (f) { return f.name !== 'node'; });
    }
    return fields.reduce(function (acc, field) {
        acc[field.name] = objectTypeFieldToNexus(graphql_1.getTypeName(type), field, prismaClient, datamodelInfo);
        return acc;
    }, {});
}
exports.objectTypeFieldsToNexus = objectTypeFieldsToNexus;
//# sourceMappingURL=objectType.js.map