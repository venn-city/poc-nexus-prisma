import { GraphQLNamedType } from 'graphql';
import { core } from 'nexus';
import { PrismaSchemaConfig, InternalDatamodelInfo } from './types';
export declare class PrismaSchemaBuilder extends core.SchemaBuilder {
    protected config: PrismaSchemaConfig;
    private datamodelInfo;
    constructor(config: PrismaSchemaConfig);
    protected missingType(typeName: string): GraphQLNamedType;
    getConfig(): PrismaSchemaConfig;
    getDatamodelInfo(): InternalDatamodelInfo;
}
export declare function isPrismaSchemaBuilder(obj: any): obj is PrismaSchemaBuilder;
